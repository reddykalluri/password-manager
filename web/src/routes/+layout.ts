// Fully client-side SPA: no SSR (all crypto runs in the browser), served as
// static files with an index.html fallback by the self-hosted instance.
export const ssr = false;
export const prerender = false;
